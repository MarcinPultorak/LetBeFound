import { AppPage } from "@/types/appPage";

const ClientsPage: AppPage = () => {
  return (
    <div>
      <h1>Klienci</h1>
    </div>
  );
};

ClientsPage.layoutType = "admin";

export default ClientsPage;
